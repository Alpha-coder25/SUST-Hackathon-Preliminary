# QueueStorm Investigator — Environment Variables
# Copy this file to .env and fill in values locally.
# NEVER commit real secret values to the repository.

# Server
PORT=8000
HOST=0.0.0.0

# Logging
LOG_LEVEL=INFO

# Optional: LLM API keys (uncomment if using an external model)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# MODEL_NAME=gpt-4o-mini
